import { RasedDatePipe } from './rased-date.pipe';

describe('RasedDatePipe', () => {
  it('create an instance', () => {
    const pipe = new RasedDatePipe();
    expect(pipe).toBeTruthy();
  });
});
