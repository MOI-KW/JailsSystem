import { CasesDatePipe } from './cases-date.pipe';

describe('CasesDatePipe', () => {
  it('create an instance', () => {
    const pipe = new CasesDatePipe();
    expect(pipe).toBeTruthy();
  });
});
